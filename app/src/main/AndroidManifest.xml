<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProvaSubLovenskyHenriscar"
        tools:targetApi="31">

        <!-- CadastroActivity agora é a tela inicial -->
        <activity
            android:name=".CadastroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ListaMedicamentosActivity (tela de listagem dos medicamentos) -->
        <activity
            android:name=".ListaMedicamentosActivity"
            android:exported="true" />

        <!-- MainActivity ainda existe, mas não é mais a principal -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>
</manifest>